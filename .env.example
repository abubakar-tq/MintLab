# Copy to `.env` for local development.
# Never commit real secrets (RPC URLs with credentials, API keys, private keys).

# Sepolia RPC (e.g., Alchemy/Infura/QuickNode)
SEPOLIA_RPC_URL=

# Etherscan API key (optional; used for contract verification)
ETHERSCAN_API_KEY=

# Local Anvil RPC (default)
ANVIL_RPC_URL=http://localhost:8545

